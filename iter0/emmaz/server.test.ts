import assert from 'assert';

/**
 * Tests for the Puzzle abstract data type
 */
describe('Server', function () {
    /**
     * Testing strategy
     * 
     *  /puzzle:
     *      partition on file name:
     *          - missing name parameter
     *          - valid file name
     *          - nonexistent file name
     */
})